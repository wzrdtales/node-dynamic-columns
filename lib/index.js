
module.exports = {

  /**
    * A non recursive way to resolve our json schema to the create query.
    */
  createQuery: function( json ) {

      var current,
          nested = [],
          query = '';

    current = Object.keys( json );

    while( current ) {

      var len = current.length;

      for( var i = 0; i < len; ++i ) {


      }
    }
  }
};
